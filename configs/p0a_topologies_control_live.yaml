run:
  experiment_id: p0a-topologies-control-live
  framework_id: coord_harness_v1
  stage: clean
  mode: research_strict
  description: Clean topology-zoo control run for Star, Balanced Tree, Linear Chain, and Complete Graph.
  output_root: outputs
  agent_count: 9
  total_billed_token_budget: 7000
  max_concurrency: 25
  capture_model_catalog_snapshot: true
  regime_thresholds:
    help_min_lift: 0.05
    collapse_max_lift: -0.05

benchmarks:
  - family: craft_mini
    stage: clean
    split: topology_zoo_control
    task_limit: 4
  - family: agentsnet_mini
    stage: clean
    split: topology_zoo_control
    task_limit: 4

model_panel:
  - alias: gemini31_flash_lite_preview
    provider: openrouter
    model_id: google/gemini-3.1-flash-lite-preview
    panel_tier: confirmatory
    temperature: 0.0
    top_p: 1.0
    max_completion_tokens: 48
    allow_auto_routing: false
    reasoning:
      effort: none
      exclude: true
    routing:
      only: [google-ai-studio]
      allow_fallbacks: false

sweep:
  benchmark_families: [craft_mini, agentsnet_mini]
  topology_presets: [star, balanced_tree, linear_chain, complete_graph]
  message_token_budgets: [96]
  model_aliases: [gemini31_flash_lite_preview]
  model_tiers: []
  baselines: [ma_ft]
  seeds: [7, 11]
